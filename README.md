# shopsys/mcp-attributes

Shared MCP exposure attributes for Shopsys Platform packages.

This package is intentionally kept lightweight so framework code can depend on
the attributes without pulling in the MCP server runtime package.
